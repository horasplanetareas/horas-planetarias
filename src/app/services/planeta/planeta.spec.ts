/*import { TestBed } from '@angular/core/testing';

import { Planeta } from './planeta';

describe('Planeta', () => {
  let service: Planeta;

  beforeEach(() => {
    TestBed.configureTestingModule({});
    service = TestBed.inject(Planeta);
  });

  it('should be created', () => {
    expect(service).toBeTruthy();
  });
});
*/